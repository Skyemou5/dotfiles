# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

source /home/mara/.config/broot/launcher/bash/br
. "$HOME/.cargo/env"
